version: 0.2

phases:
  integrationTest:
    commands:
      - npm install
      - npm run test
  deploy:
    commands:
      - npm install
      - npm run sls -- deploy -s $STAGE -r $REGION
  acceptanceTest:
    commands:
      - npm install
      - npm run acceptance

cache:
  paths:
    - node_modules
